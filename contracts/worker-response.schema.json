{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://storpt.local/contracts/worker-response.schema.json",
  "title": "StoRpt Excel Worker response",
  "type": "object",
  "additionalProperties": false,
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["success", "error"] },
    "operation": { "type": "string", "enum": ["analyze", "write"] },
    "outputPath": { "type": "string", "minLength": 1 },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sheetIndex", "sheetName", "periods", "latestPeriod"],
      "properties": {
        "sheetIndex": { "type": "integer", "minimum": 0 },
        "sheetName": { "type": "string", "minLength": 1 },
        "periods": { "type": "array" },
        "latestPeriod": { "type": "object" }
      }
    },
    "errors": {
      "type": "array",
      "items": { "$ref": "error-codes.json#/$defs/error" }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "status": { "const": "success" } } },
      "then": { "required": ["operation", "metadata"] }
    },
    {
      "if": {
        "properties": {
          "status": { "const": "success" },
          "operation": { "const": "analyze" }
        },
        "required": ["status", "operation"]
      },
      "then": { "not": { "required": ["outputPath"] } }
    },
    {
      "if": {
        "properties": {
          "status": { "const": "success" },
          "operation": { "const": "write" }
        },
        "required": ["status", "operation"]
      },
      "then": { "required": ["outputPath"] }
    },
    {
      "if": { "properties": { "status": { "const": "error" } } },
      "then": { "required": ["errors"], "not": { "required": ["outputPath"] } }
    }
  ]
}
